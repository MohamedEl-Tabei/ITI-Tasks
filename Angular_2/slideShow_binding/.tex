% --------------------------------- angular 2 -------------------------------- %
binding one way 
binding two way 
directive
control flow
====================
Decorator => MetaData => information
==================
@Component({
    templateUrl:"",
    selector:"app-first"
}) لازم
========================
الوحيد الي يتنده في ال
index.html
هوه 
App rout
=======================
stand alone Component
كل واحده شايله حاجتها
=======================
lazy loading 
=======================
@Component({`'
    templateUrl:"",
    selector:"app-first",
    providers:[
        services
    ]
})
==========================
ng generate component second

create component by command
============================
منغير 
testing

ng g c third --skip-tests
===========================
مينفعش تنده على 
component 
جوه نفسها
infinity loop
==========================
styles.css
الي جوه 
app
هو 
css 
الوحيد الي بيسمع في كل 
components
============================
styles.css
اول واحد بيشتغل وبعد كده كل
component.css
بيعمل 
override 
عليه
=============================
interpolition
<p>{{3+5}}</p>
      8
<input type="text" value="{{ userName }}" />
==============================
one way binding [from TS to HTML] 
مع
void tag 
بس 
<input type="text" [value]="userName" />
=======================================
event binding
<button (click)="clickMe()">Click me</button>
======================================
Two way binding ==> [from html to ts && from ts to html] 

----
(input)="fun($event)" 
$event => reserved word

<input (input)="getText($event)" type="text" />

--by angular
default is input event
banana in box 
[(ngModel)] => reserved model

<input [(ngModel)]="text" type="text" />

@Component({
  templateUrl: './first.component.html',
  styleUrl: './first.component.css',
  selector: 'app-first',
  imports: [FormsModule],
})
